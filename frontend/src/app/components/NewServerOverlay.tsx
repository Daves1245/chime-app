'use client';

import React, { FC } from 'react';

interface NewServerOverlayProps {
  onClose: () => void;
  onAddServer: () => void;
}

const NewServerOverlay: FC<NewServerOverlayProps> = () => {
  return <>{/* Component implementation would go here */}</>;
};

export default NewServerOverlay;
